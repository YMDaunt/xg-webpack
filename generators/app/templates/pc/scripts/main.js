'use strict'

import Vue from 'vue'
import axios from 'axios'
import user from 'user'
// import layer from 'layer'; // desc: 弹出层插件

import '<%= stylePath %>'

new Vue({
    el: '#app',
    data: {},
    mounted: function () {
    },
    methods: {
    }
})
